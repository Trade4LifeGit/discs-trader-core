version: '2'
services:
  discstradercore-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/discstradercore-mongodb-config --bind_ip 0.0.0.0
  discstradercore-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  discstradercore-mongodb-config:
    image: mongo:4.2.7
    container_name: discstradercore-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
